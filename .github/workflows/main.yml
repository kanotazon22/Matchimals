name: Unzip File

on: [push]

jobs:
  unzip_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Unzip file
        run: unzip matchimals01.zip -d thu_muc_giai_nen
